{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node",
            "request": "launch",
            "name": "Launch API",
            "program": "${workspaceFolder}\\api\\build\\app.js",
            "preLaunchTask": "tsc: build - api\\tsconfig.json",
            "outFiles": [
                "${workspaceFolder}/api/build/**/*.js"
            ]
        }
    ]
}